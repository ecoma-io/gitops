apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ingress

helmCharts:
  - name: cloudflared
    repo: https://charts.kubito.dev
    releaseName: cloudflared
    namespace: ingress
    version: 1.7.7
    valuesFile: cloudflared.values.yaml

resources:
  - sealed-secret.yaml
