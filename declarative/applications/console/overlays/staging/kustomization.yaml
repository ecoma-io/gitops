apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ecoma-staging
resources:
  - ../../base
patches:
  - target:
      group: traefik.io
      version: v1alpha1
      kind: Middleware
      name: console-env-header
    patch: |-
      apiVersion: traefik.io/v1alpha1
      kind: Middleware
      metadata:
        name: console-env-header
      spec:
        headers:
          customResponseHeaders:
            X-Environment: staging
  - target:
      group: traefik.io
      version: v1alpha1
      kind: IngressRoute
      name: console
    patch: |-
      - op: replace
        path: /spec/routes/0/match
        value: "Host(`staging-console.ecoma.io`)"
